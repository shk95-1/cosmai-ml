# 통합 데이터로 만들 수 있는 모델

`MODELS.md`는 관세청 매출을 앵커로 삼는 A~E 모델을 다룬다. 이 문서는 **강석현 님의 통합
DB를 DGX로 옮긴 뒤**(2026-08-22T02:42 UTC 시점 고정) 그 데이터로 실제로 무엇이 되는지를
적는다. 판단은 전부 아래 실측 수치에 근거한다.

## 손에 있는 것

DGX `cosmai_integrated`, 12개 테이블, 전 테이블 원본과 행 수 일치 확인.

| 테이블 | 행 | 비고 |
|---|---:|---|
| `rank_snapshot` | 80,102 | 4개 소스, **전역 시간 버킷 47개**, 2026-08-18 ~ 08-22 |
| `price_point` | 67,796 | 할인 **상태**인 행 39,275. 할인 **전이**는 33건뿐 — 아래 참조 |
| `review` | 15,817 | 평점·작성시각 100%, 본문 14,245 |
| `review_topic` | 10,476 | **사이트가 계산한 키워드** |
| `review_answer` | 7,494 | 설문형 응답 |
| `product` | 4,804 | 브랜드 3,875 / **고유 브랜드 814** / **성분 115** |
| `review_summary` | 1,326 | **사이트가 LLM으로 쓴 글** |
| `review_stats` | 664 | 평점 분포 |
| `new_product` | 295 | |

소스별 랭킹 커버리지:

| 소스 | 랭킹 행 | 제품 | 리뷰 | 리뷰가 붙은 제품 |
|---|---:|---:|---:|---:|
| oliveyoung | 52,000 | 2,873 | 10,577 | **38** |
| daisomall | 16,599 | 1,514 | 3,658 | **17** |
| glowpick | 10,060 | 240 | 1,582 | 949 |
| hwahae | 1,443 | **30** | 0 | 0 |

## 먼저 읽어야 할 세 가지 함정

이걸 모르고 만들면 좋은 점수가 나오는데 아무 의미가 없다.

**1. 리뷰는 하나의 데이터셋이 아니다.** oliveyoung은 제품 38개에 리뷰 10,577건(제품당 278건),
glowpick은 제품 949개에 1,582건(제품당 1.7건)이다. **모양이 완전히 다르다.** 합쳐서 "리뷰
15,817건"으로 다루면, 사실상 oliveyoung 제품 38개를 설명하는 모델을 만들어놓고 시장을
설명한다고 말하게 된다.

**2. `review_topic`과 `review_summary`는 우리가 만든 것이 아니다.** `review_topic`은 사이트가
계산한 키워드고, `review_summary`는 사이트가 LLM으로 쓴 문장이다. 전자를 ABSA의 정답으로
쓰고 그걸로 평가하면 순환이다. 후자를 사용자 발화로 학습시키면 **다른 모델의 출력을 사람
말인 줄 알고 배운다.**

**3. 시간이 4일뿐이다.** 전역 시간 버킷 47개(소스별로는 다르다 — 43은 oliveyoung 하나의 수다). 시계열을 타깃으로 하는 모든 모델은 지금 학습할 수
없다. "지금 가능"과 "쌓이면 가능"의 경계는 데이터 양이 아니라 **타깃이 시간을 필요로 하는가**다.

여기에 `trend-radar#19`가 겹친다 — 파서가 사이트 구조 변경으로 못 읽으면 예외가 아니라 빈
결과를 반환하고 실행은 `ok`로 남는다. 어느 구간으로 학습하든 `run.status`와
`run_source.errors`로 구멍을 먼저 확인하고 명시적으로 제외해야 한다.

---

## 지금 학습 가능

타깃이 시간을 요구하지 않는 것들이다.

### C1. 속성 기반 감정분석 (ABSA)

| | |
|---|---|
| 단위 | 리뷰 1건 |
| Y | 속성별 감성 (발림성·지속력·향·자극도·보습·가성비 등) |
| X | 리뷰 본문 14,245건 |
| 상태 | **가능. 다만 정답을 어디서 얻느냐가 전부다** |

**`review_answer` 7,494건이 여기서 가장 저평가된 자산이다.** 설문형 응답이라 이미 구조화된
속성 평가에 가깝다. 사이트가 만든 `review_topic`을 정답으로 쓰는 것보다, 이쪽을 약지도학습
신호로 쓰는 편이 순환을 피한다.

한국어 화장품 도메인이므로 범용 감성모델은 "자극이 없어요"를 부정으로 읽는 등 도메인 반전에
약하다. 소스 인벤토리의 AI Hub 속성기반 감정분석(F1, 조건부 무료)이 가장 가까운 학습셋이다.

**평가 함정**: `review_topic`으로 학습하고 `review_topic`으로 평가하면 사이트 알고리즘을
복제한 것이지 감성을 배운 것이 아니다. 최소 200~300건은 손으로 라벨링해서 홀드아웃을 따로
만들어야 숫자에 의미가 생긴다.

### C2. 크로스 플랫폼 제품 매핑

| | |
|---|---|
| 단위 | 제품 쌍 (소스 A × 소스 B) |
| Y | 같은 제품인가 |
| X | 제품명, 브랜드(3,875건/814개), 용량, 가격대 |
| 상태 | **가능하고, 다른 거의 모든 것의 전제다** |

지금 4,804개 제품이 소스별로 흩어져 있고 **소스 간 동일성이 구현되어 있지 않다.** 화해의
제품과 다이소몰의 같은 제품은 서로 다른 행이다. 이걸 풀지 않으면 소스별로 따로 노는 모델
네 개가 나올 뿐 통합 신호가 안 나온다.

임베딩 + 퍼지 매칭이 맞는 접근이다. 한국어 제품명은 `[브랜드] 제품명 용량 (기획)` 같은
형태가 많아 정규화가 절반이다.

**진짜 작업은 모델이 아니라 평가셋이다.** 정답 쌍이 없으면 정밀도 90%와 60%를 구별할 수
없다. 손으로 300쌍 정도(양성·음성 섞어) 만들어두는 게 이 모델의 실제 비용이고, 그게 없으면
"매핑했다"는 말은 검증 불가능한 주장이다.

### C3. 리뷰 진위 이상탐지 — 작성자 식별 없이 가능한 부분

| | |
|---|---|
| 단위 | 제품 × 시간 |
| Y | 이상 여부 (비지도) |
| X | `written_at` 분포, `review_stats`의 별점 분포, 평점-본문 불일치 |
| 상태 | **부분적으로 지금 가능** |

`written_at`이 15,817건 전부 있으므로 **버스트 탐지**는 지금 된다. 정상 리뷰 도착은 대체로
포아송에 가깝고, 조작은 짧은 창에 몰린다. `review_stats`의 별점 분포로 U자형(5점과 1점만
많은) 이상도 잡힌다.

**작성자 단위 어뷰징은 다른 문제다.** 그건 `author_hash`가 필요한데, 그 컬럼은
`trend-radar#20`으로 "무염 SHA-256이고 역추적 가능하며 읽는 코드가 없다"고 이슈를 올려둔
상태다. **이 모델이 그 컬럼의 유일한 정당한 소비자다.** 즉 이슈의 결론이 바뀐다 —
"쓰지 않으니 지우자"가 아니라 "쓸 것이므로 HMAC으로 재설계하자"가 된다. 어느 쪽이든
지금 상태(무염, 역추적 가능)로 두는 선택지는 없다.

### C4. 근거 리뷰 선별

| | |
|---|---|
| 단위 | 리뷰 1건 |
| Y | 근거로 쓸 만한가 |
| X | 길이, 구체성, 속성 언급 수(C1 출력), 평점과의 정합 |
| 상태 | **가능. 제품 목표에 가장 직접적** |

카드에 "근거 3건"을 붙이려면 15,817건 중 무엇을 보여줄지 골라야 한다. 무작위나 최신순은
근거가 아니다. C1이 나오면 그 출력을 특징으로 쓸 수 있다.

### C5. 성분 파싱 (INCI 토큰화)

| | |
|---|---|
| 단위 | 제품 1건 |
| 출력 | 성분 리스트 |
| X | `product.ingredients` **115건** (oliveyoung 전용) |
| 상태 | **프로토타입만 가능. 모델링은 불가** |

프로젝트 통틀어 처음으로 성분 데이터가 존재한다. 다만 **115건은 oliveyoung 제품의 4%,
전체의 2.4%**다. 파서를 만들고 검증하기엔 충분하고, 성분 트렌드를 말하기엔 턱없이 모자란다.

사이트 자체 구분자(`[세럼] … [마스크]`)가 문자열에 섞여 있어 그것부터 걷어내야 한다.
**이 파서가 아래 L5의 전제**이므로, 지금 만들어두면 데이터가 쌓였을 때 기다릴 필요가 없다.

---

## 데이터가 쌓여야 가능

타깃이 시간을 요구한다. 지금 전역 시간 버킷 47개 / 4일이고, **제품별 달력 연속 관측은 최장 5일**이다.

| 모델 | 필요한 것 | 언제 |
|---|---|---|
| L1. 랭킹 변화·급상승 조기감지 | 모멘텀 창 + 예측 지평 + 홀드아웃 | 제품별 **달력 연속 관측 22일**. 행 수가 아니라 날짜 — `model_b.py`가 이걸 틀려서 삭제됐다 |
| L2. 할인 → 랭킹 반응 | 할인 **이벤트**와 그 후 랭킹 궤적 | 아래 참조 |
| L3. 신제품 성공 예측 | 출시 시점 관측 + 결과 확정까지 | 2~3개월 |
| L4. 제품 수명·이탈 (생존분석) | 상위권 진입·이탈이 관측될 시간 | 1개월+ |
| L5. 성분 트렌드 | C5 파서 + 성분 커버리지 + 시간 | 커버리지가 병목 |

**L2는 가깝지 않다. 2026-08-22 정정.** 이 자리에 "할인 39,275건이 관측되어 이벤트가 이미
풍부하므로 L1보다 빠를 수 있다"고 적었는데, 틀렸다. 39,275는 **할인 상태인 행**이지
이벤트가 아니다. 실제로 세어보니 **상태 전이는 29개 제품에서 33건**(시작 10, 종료 23)뿐이고
**전부 oliveyoung**이며 나머지 세 소스는 0건이다.

행을 세고 이벤트라고 부른 것이 오류의 전부다. 33건으로는 할인 반응을 추정할 수 없고,
한 소스에만 있으므로 그 소스의 프로모션 정책을 학습하게 된다. **L2는 L1보다 빠르지 않다.**

**L1에 반드시 붙일 것**: 랭킹은 자기상관이 극도로 강하다. **"내일 순위 = 오늘 순위"
persistence 베이스라인을 이기지 못하면 신호를 찾은 것이 아니다.** `model_a.py`에서 창
하나만 믿었다가 9개 창 중 3개에서 지는 걸 확인했다. 같은 실수를 반복하지 않으려면 처음부터
다중 창으로 평가한다.

---

## 목록에 없던 것 중 해볼 만한 것

### N1. 소스 간 랭킹 통합 (rank aggregation)

C2가 풀리면 같은 제품에 대해 **서로 다른 4개의 순위**가 생긴다. 각 사이트의 랭킹은 자기
트래픽과 정책이 섞인 잡음 섞인 관측이다. Bradley-Terry나 Plackett-Luce로 잠재 인기도 하나를
추정하면, 사이트 하나의 편향에 휘둘리지 않는 단일 지표가 나온다.

hwahae가 제품 30개뿐이고 oliveyoung이 2,873개인 상황에서 특히 유효하다 — **커버리지가
다른 관측을 어떻게 합치느냐**가 문제의 본질이고, 그게 이 방법이 푸는 것이다.

### N2. 카테고리 수요 나우캐스팅 — 관세청과 잇는 다리

지금 통합 데이터에는 외부 앵커가 없다. `MODELS.md`가 쓰는 관세청 월별 매출이 유일한 외부
기준이다. 랭킹·가격을 카테고리 단위로 집계해 지수를 만들고 그 월별 매출과 맞춰보면,
**우리 신호가 실제 시장과 상관이 있는지**를 처음으로 검증할 수 있다.

Model E에서 학계 축이 신호가 없다고 나왔던 것과 같은 검증이다. 결과가 "상관 없음"이어도
그건 답이지 실패가 아니다.

### N3. 브랜드 단위 모델

`product.brand`에 **814개 브랜드**가 있다. 제품이 아니라 브랜드를 단위로 두면 표본이
4,804에서 814로 줄지만 **행동이 훨씬 안정적**이다. 출시 주기, 가격 포지셔닝, 상위권 진입
비율. L3(신제품 성공)의 가장 강한 특징이 "그 브랜드가 과거에 성공시켰는가"이므로, 이 모델은
L3의 부품이기도 하다.

### N4. 소스 간 가격 정합성

C2 이후, 같은 제품이 사이트마다 다른 가격·할인율로 나타난다. 단순 비교지만 실무적으로
바로 쓰인다. 모델이라기보다 지표에 가깝고, 그래서 빨리 나온다.

### N5. 신제품 콜드스타트 랭킹

L3와 다르다. L3는 "성공할 것인가"(결과 확정 필요)이고, 이건 **출시 시점 특징만으로 첫 주
순위를 예측**한다. 결과를 기다릴 필요가 없어 `new_product` 295건에 랭킹을 붙이는 순간
검증할 수 있다. L3로 가는 징검다리.

### N6. `review_topic` 드리프트 추적

사이트가 계산해준 키워드가 10,476건 있다. 우리가 만든 게 아니라서 모델의 정답으로는 못
쓰지만, **시간에 따른 분포 변화**를 보는 데는 공짜로 쓸 수 있다. 어떤 속성 언급이 늘고
주는지에 대한 가장 싼 신호다. 단, 사이트가 알고리즘을 바꾸면 그것도 변화로 보인다.

---

## 권장 순서

1. **C2 (제품 매핑)를 먼저.** N1·N4가 여기 걸려 있고, 안 하면 소스별로 따로 노는 모델
   네 개가 된다. **평가셋 300쌍을 손으로 만드는 것부터**가 실제 시작이다.
2. **C1 (ABSA)을 `review_answer` 기반으로.** `review_topic`을 정답으로 쓰지 않는다.
3. **C5 (성분 파서)를 지금 만들어둔다.** 데이터가 아니라 코드가 병목이므로, 커버리지가
   올라오는 시점에 기다리지 않게 된다.
4. **L2는 순서에서 내린다.** 전환이 33건뿐이고 전부 한 소스라, 세어본 결과가 이 항목을
   앞이 아니라 뒤로 보냈다.
5. **N2 (관세청 대조)로 신호의 실체를 검증한다.** 이걸 하지 않으면 나머지 전부가 "그럴듯한
   내부 지표"로 남는다.

## 모든 모델에 공통으로 붙일 것

- **`Y`와 `X`가 같은 세는 과정에서 나오면 안 된다.** 외부 앵커는 관세청 매출과 랭킹 둘뿐이다.
- **persistence 베이스라인을 반드시 같이 보고한다.** 이기지 못하면 결과가 아니다.
- **창 하나로 판단하지 않는다.** 여러 (지평, 홀드아웃) 조합에서 지는 창을 명시한다.
- **학습 구간에 수집 구멍이 있는지 `run_source`로 먼저 확인한다.**
- **리뷰 기반 모델의 결론에는 "제품 몇 개를 설명하는가"를 반드시 함께 적는다.**
