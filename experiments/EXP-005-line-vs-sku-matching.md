# EXP-005 — 라인 기준으로 라벨링하면 정밀도가 달라지는가

## Identity and status

- Experiment ID: `EXP-005`
- Type: `OTHER` (measurement, overnight)
- Status: `RUNNING`
- Related: `EXP-004`
- Owner: hyeonjunni
- Created at: 2026-08-23T00:35+09:00
- Last executed at: —

**실행 전에 선언한다.** 가설, 반증 조건, 비교 방식은 아래에 고정되며 결과가 나온 뒤에도
수정하지 않는다.

## Question

`EXP-004`에서 SKU 기준 정밀도는 재검토 후 262건 중 2건이었다. 실패의 상당 부분이 매처가
아니라 문제 설정 — oliveyoung 제품 행의 27.6%가 여러 SKU를 묶은 판매 리스팅 — 에서 왔다는
것도 확인됐다.

다운스트림이 실제로 요구하는 동일성은 대부분 SKU 가 아니라 제품 라인이다. 그렇다면 같은
후보를 라인 기준으로 라벨링하면 정밀도가 쓸 만한 수준으로 올라가는가.

## Hypothesis

`[가설]` 같은 후보 쌍을 제품 라인 기준으로 라벨링하면, 상위 점수 구간에서 SKU 기준보다
현저히 높은 정밀도가 나온다. 매처는 라인 동일성을 이미 찾고 있었고 SKU 기준으로 채점당해
왔을 뿐이다.

## Falsification condition

실행 전에 선언한다. 다음 중 **하나라도** 성립하면 반증이다.

1. `score >= 1.00` 에서 라인 기준 정밀도가 **50% 미만**. 그 정도로는 라인 매칭으로 바꿔도
   다운스트림에 쓸 수 없고, 문제는 정의가 아니라 매처에 있다는 뜻이 된다.
2. 라인 기준 정밀도가 SKU 기준 대비 상위 구간에서 **2배 미만** 개선. 정의를 바꾼 효과가
   그 정도면 바꿀 이유가 약하다.

## 비교 방식

같은 627개 후보를 **두 번** 라벨링한다. 같은 쌍, 같은 점수 없는 입력, 기준만 다르다.

- **SKU 기준** — `EXP-004`와 동일. 용량·수량·기획·리필·변형이 다르면 `n`
- **라인 기준** — 같은 제품 라인이면 `y`. 용량·수량·기획·리필 차이는 무시.
  제형(세럼/토너/크림)과 향·타입이 다르면 `n`. 다변형 리스팅은 A가 그 리스팅에 포함되는
  변형이면 `y`

`EXP-004`는 298쌍 표본이었다. 이번에는 627쌍 전체를 라벨링하므로 상위 구간의 신뢰구간이
좁아진다 — `>= 1.20` 이 12건에서 138건이 된다.

## Exit condition

627쌍 × 2기준 = 1,254 라벨. 한 번 실행하고 끝낸다. 기준을 조정해 다시 돌리지 않는다.

## Scope

### Included

- `matches.csv` 627쌍 전체, commit `a95e2b0` 산출물
- 두 기준 각각의 구간별·누적 정밀도와 Wilson 구간
- 두 기준이 어긋나는 쌍의 목록 (그 자체가 리스팅 문제의 표본)

### Excluded

- 재현율. 표본이 여전히 매처의 후보에서 나오므로 측정 불가.
- 매처 수정. 이번에는 측정만 한다.
- 사람 재검토. 실행 후 별도로 하며, `EXP-004`가 보였듯 이것 없이는 숫자를 확정할 수 없다.

## Environment

- 라벨링: `claude -p --model sonnet`, 점수를 제거한 입력, 배치당 50쌍
- 집계: `evaluate_matches.py`
- 호스트: desktop-u75tsal. DGX 는 쓰지 않으므로 Minecraft 와 무관하다.

## Procedure

1. `matches.csv` 627쌍에서 점수를 제거한 입력을 만든다.
2. SKU 기준으로 13배치 라벨링.
3. 라인 기준으로 13배치 라벨링. 같은 입력, 다른 기준.
4. 각각 병합하고 정밀도를 계산한다.
5. 두 기준이 어긋나는 쌍을 따로 뽑는다.

## Observations

```text
[측정] 실행 후 기록
```

## Interpretation

```text
[추론] 실행 후 기록
```

## Result

- Outcome: `PENDING`
- Falsification condition met: `NOT TESTED`
- Exit condition met: `NO`
- Known limitations: 라벨이 다시 모델 생성이다. `EXP-004`에서 `y` 10건 중 8건이 사람
  재검토로 뒤집혔고, 오류가 무작위가 아니라 한 방향으로 쏠렸다. 이번 결과도 사람 재검토
  전에는 확정이 아니다.
